# Lab A — Classical Shootout (Logistic vs SVM vs Random Forest)

> Run after module 06. Same dataset, three classifiers, head to head.

**Status:** stub.

Planned contents:
- One toy 2D dataset (e.g., moons or concentric circles, then a noisier version), one clean baseline dataset (e.g., the wine or breast-cancer UCI set).
- Plot all three models' decision boundaries side by side on the 2D data.
- Compare on the higher-D dataset by accuracy, calibration, and training time.
- Conclusion section: a short table — *for this kind of data, this one wins because…*
- Code uses your `from_scratch` implementations from modules 02, 04, 06 (with scikit-learn allowed only for cross-validation and metric helpers — never for the classifier itself).

What you'll learn:
- The "every algorithm is somebody's best algorithm" lesson — when one model decisively beats the others, the data is telling you something about its geometry.
